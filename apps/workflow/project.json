{
  "name": "workflow",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/workflow/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/workflow",
        "main": "apps/workflow/src/index.ts",
        "tsConfig": "apps/workflow/tsconfig.app.json",
        "webpackConfig": "apps/workflow/webpack.config.js"
      },
      "configurations": {
        "production": { "optimization": false, "outputHashing": "none" }
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "options": { "buildTarget": "workflow:build" }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "options": { "jestConfig": "apps/workflow/jest.config.ts" }
    },
    "lint": { "executor": "@nx/eslint:lint" }
  },
  "tags": ["scope:app", "service:workflow"]
}
