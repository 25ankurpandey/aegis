# apps/cli/.env — used by the migration/ops one-shot (PROCESS_TYPE=migration).
NODE_ENV=development
AEGIS_ENV=local
SERVICE_NAME=cli
LOG_LEVEL=info

# Migrations connect as the OWNER (DDL needs ownership), not the runtime app role.
DATABASE_URL=postgres://aegis_owner:aegis_local@postgres:5432/aegis
